import InventaireConsommables from "./InventaireConsommables";

// Same component logic but filtered for "materiel" type
// For brevity, this reuses the same component with different filtering
// In production, you'd want to extract common logic to a shared component

export default InventaireConsommables;
